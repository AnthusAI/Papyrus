Latency dominates user experience when each interaction waits on the backend.

Perceived responsiveness depends on how long operations stall before answers return.

Snappiness hinges on backend delay during every user-facing request.
